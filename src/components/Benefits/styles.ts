import styled from 'styled-components'

export const Container = styled.section`
  height: 3.25rem;
  width: 100%;
  max-width: var(--max-width);
  margin: var(--margin-global);
  padding: 0 6rem;

  display: flex;
  justify-content: space-between;
  align-items: center;
`

export const BenefitsOptions = styled.span`
  height: 1.5rem;
  font-size: 0.9rem;

  display: flex;
  align-items: center;
  gap: 1rem;
`
